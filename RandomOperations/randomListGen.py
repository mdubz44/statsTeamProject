import random

class RandomListGen:

    @staticmethod
    def listGen(start, end, num):
        result = []
        for i in range(num):
            res.append(random.randint(start, end))

        return result