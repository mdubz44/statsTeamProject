import  random
from RandomGenerator import randomPick

class RandomNumsFromList:

    @staticmethod
    def NumsFromListNoSeed(alist):
        alist = []
        newList = []
        for i in range(random.random):
            newList += Pick(alist)
        return newList